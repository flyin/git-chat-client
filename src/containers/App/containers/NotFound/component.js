import React from 'react';
import Helmet from 'react-helmet';

const NotFound = () => (
  <div>
    <Helmet title="Page not found" />

    Not found container here
  </div>
);

NotFound.propTypes = {};
NotFound.defaultProps = {};

export default NotFound;
